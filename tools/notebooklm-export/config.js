/** IDs de fonte (ex.: "PHB", "XGE"). Array vazio = incluir todas. */
export const ALLOWED_SOURCES = [];

/** Diretório de saída relativo à raiz do repositório. */
export const OUTPUT_DIR = "tools/notebooklm-export/output";

/** Tamanho máximo de um arquivo .md antes de dividir em partes. */
export const MAX_FILE_CHARS = 400_000;
